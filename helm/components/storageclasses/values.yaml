rwoLocality:
  name: linbit-rwo-locality
  replicaCount: 2
  storagePool: lvm-thin
  fsType: xfs
  isDefault: true
  quorum: io-error
  onNoQuorum: io-error

rwxVirt:
  name: linbit-rwx-virt
  replicaCount: 2
  storagePool: lvm-thin
  allowTwoPrimaries: true
  quorum: io-error
  onNoQuorum: io-error
